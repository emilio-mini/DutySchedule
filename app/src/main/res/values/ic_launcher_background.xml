<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only the adaptive launcher icon uses this. The splash draws @drawable/ic_launcher_animated,
         which wraps the foreground on its own, so this plate never reaches the splash screen. -->
    <color name="ic_launcher_background">#FFFFFF</color>
</resources>